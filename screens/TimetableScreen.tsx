import React from 'react';
import {StyleSheet, View} from 'react-native';

interface TimetableScreenProps {

}

function TimetableScreen({}: TimetableScreenProps) {
  return (
    <View></View>
  )
}

const styles = StyleSheet.create({});

export default TimetableScreen;